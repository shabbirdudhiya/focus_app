name: Build Focus App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Run Tests
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install MinGW
      run: |
        choco install mingw -y --no-progress
        echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $GITHUB_PATH

    - name: Compile Tests
      shell: cmd
      run: |
        cd tests
        g++ -static -static-libgcc -static-libstdc++ -D_UNICODE -DUNICODE -o test_basic.exe test_basic.cpp
        cd ..

    - name: Run Unit Tests
      shell: cmd
      run: |
        tests\test_basic.exe
        echo All tests passed!

  build:
    name: Build Executable
    needs: test
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install MinGW
      run: |
        choco install mingw -y --no-progress
        echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $GITHUB_PATH

    - name: Build Focus App (Full Version)
      shell: cmd
      run: |
        g++ -static -static-libgcc -static-libstdc++ -D_UNICODE -DUNICODE -o focus_app.exe focus_app_full.cpp -luser32 -lgdi32 -lcomctl32 -lshell32 -lpsapi

    - name: Upload executable
      uses: actions/upload-artifact@v4
      with:
        name: focus_app_exe
        path: focus_app.exe
        retention-days: 30

    - name: Create Release (on main branch)
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v2
      with:
        files: focus_app.exe
        name: Focus App v1.3
        tag_name: v1.3
        body: |
          Lightweight Windows focus app with modern design features.
          
          Features:
          - Modern Clock View with current time and date
          - Focus View with Pomodoro timer
          - Theme Toggle (Dark/Light mode)
          - Fullscreen mode
          - Floating timer window (always on top)
          - App blocking with warnings
          - Task tracking
          - Session tracking with progress dots
          - Configurable durations
          
          All tests pass before building!
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
